// Shared height for every dashboard card that renders a variable-length
// list, so a card with a lot of data scrolls internally instead of pushing
// the rest of the page down, and cards in the same row line up.
export const LIST_CARD_HEIGHT = "h-80";
